//Ascii value of the character

#include <stdio.h>
int main() {  
    char c;
    printf("Enter a character: ");
    scanf("%c", &c);  
    

    printf("ASCII value = %d", c);//ascii value of upper &lower case character are different like a=97,A=65
    
    return 0;
}